document.querySelectorAll('.bars').forEach(el=>el.innerHTML=Array.from({length:25},(_,i)=>`<i style="--h:${10+Math.abs(Math.sin(i*.83))*34}px"></i>`).join(''));
document.querySelectorAll('.sound-card').forEach(card=>card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--x',`${e.clientX-r.left}px`);card.style.setProperty('--y',`${e.clientY-r.top}px`)}));
document.querySelectorAll('.memory').forEach(card=>card.addEventListener('click',()=>{document.querySelectorAll('.memory').forEach(x=>x.classList.remove('selected'));card.classList.add('selected');const t=document.querySelector('#selectionTitle');if(t)t.textContent=card.dataset.title||'Selected memory'}));
document.querySelectorAll('[data-toggle-play]').forEach(btn=>btn.addEventListener('click',()=>btn.textContent=btn.textContent.includes('Play')?'■ Stop':'▶ Play'));
const world=document.querySelector('.world');if(world){let down=false,x=0,y=0,rx=0,ry=0;world.addEventListener('pointerdown',e=>{down=true;x=e.clientX;y=e.clientY;world.setPointerCapture(e.pointerId)});world.addEventListener('pointermove',e=>{if(!down)return;ry+=(e.clientX-x)*.12;rx-=(e.clientY-y)*.08;x=e.clientX;y=e.clientY;world.style.transform=`translate(-50%,-50%) rotateX(${rx}deg) rotateY(${ry}deg)`});world.addEventListener('pointerup',()=>down=false)}
